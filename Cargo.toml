[package]
name = "spozalon"
version = "0.1.0"
edition = "2024"

[dependencies]
cpal = "0.18"
tokio = { version = "1", features = ["full"] }
clap = { version = "4", features = ["derive"] }
anyhow = "1"

[profile.release]
opt-level = 3
lto = true
